spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/invertir_ya_db
    username: postgres
    password: sistemas

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/